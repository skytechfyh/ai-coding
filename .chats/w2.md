# Instructions

## constitution

这是针对 ./w2/db_query 项目的

- 使用Ergonomic Python 风格来编写代码，前端使用typescript
- 前端后端要有严格的类型标注
- 使用pydantic来定义数据模型
- 所有后端生成的 JSON 数据，使用 camelCase格式
- 不需要 authentication，任何用户都可以使用



## 基本思路

这是一个数据库查询工具，用户可以添加一个 db url，系统会连接到数据库，获取数据库的 metadata，然后将数据库中的 table 和 view的信息展示出来，然后用户可以自己输入 sql 查询，也可以通过自然语言来生成sql查询。

基本想法: 
- 数据库连接字符串和数据库的 metadata 都会存储到 sqlite 数据中，我们可以根据 postgres 的功能来实现查询系统中的表和视图的信息，然后用 LLM 将这些信息转换成 json 格式，然后存储到 sqlite 数据库中。 这个信息以后可以复用。
- 当用户使用 LLM 来生成 sql 查询时，我们可以把系统中的表和视图的信息作为 context 传递给LLM，然后 LLM 会根据这些信息来生成 sql 查询。
- 任务输入的 sql 语句， 都需要经过 sqlparse 解析，确保语法正确，并且仅包含 select 语句。如果语法不正确，需要给出错误信息。
    - 如果查询不包含 limit 子句，则默认添加 limit 1000 子句。
- 输出的格式是 json，前端将其组织成表格，并显示出来。

后端使用 Python(uv) / FastApi / sqlglot / openai sdk 来实现。
前端使用 React / refine 5 / tailwind / ant design 来实现。 sql editor 使用 manaco editor来实现。

OpenAI API Key在环境变量OPENAI_API_KEY 中。数据库连接和metadata 存储在 sqlite 数据库中，放在 ～/.db_query/db_query.db中。

后端 API 需要支持 cors，允许所有 origin。
大致的 API 设计如下：
```bash
# 获取所有已存储的数据库
GET /api/v1/dbs
# 添加一个数据库
PUT /api/v1/dbs/{name}

{
  "url": "postgres://postgres:postgres@localhost:5432/postgres"
}

# 获取一个数据库的 metadata
GET /api/v1/dbs/{name}

# 查询某个数据库的信息
POST /api/v1/dbs/{name}/query

{
  "sql": "SELECT * FROM users"
}

# 根据自然语言生成 sql
POST /api/v1/dbs/{name}/query/natural

{
  "prompt": "查询用户表的所有信息"
}
```

## 测试
仔细阅读 ./w2/db_query 下面的代码，然后运行后端和前端，根据@w2/db_query/fixtures/test.rest 用 curl 测试后端已实现的路由；然后用 playwright 开前端进行测试，任何测试问题，think ultra hard and fix


## 优化UI
- 按照 apple website 的设计风格，think ultra hard，优化 UI 和 UX。
- 可以按照datagrip这种数据库工具的设计风格来设计 UI，保持简洁和高效。
- 前端使用 refine 5 来实现，保持 UI 的一致性和美观。
- sql editor 使用 manaco editor 来实现，提供良好的 sql 编辑体验。


## 添加MySQL 支持
这是针对 ./w2/db_query 项目的，现在需要添加功能:
- 生成的task.md 放到 ./specs/002-mysql-support/task.md 中，按照上面的格式来编写。
- 参考 ./w2/db_query/db.py 中的 PostgresSQL实现， 实现 MySQL的metadata 获取和查询功能。同时支持自然语言生成SQL查询。需要注意 MySQL 和 PostgreSQL 在 SQL 语法上的差异，确保生成的 SQL 查询能够在 MySQL 中正确执行。

## 测试 MySQL 支持
- 在 ./w2/db_query/fixtures/test_mysql.rest 中添加针对 MySQL 的测试用例，然后运行测试。如果后端测试OK，那么打开后端和前端，使用 playwright 来测试 MySQL 的功能，确保 UI 和 UX 都没有问题。